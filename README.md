# FormFlow

Predictable form state, validation, and submission flows for Jetpack Compose.

## Status
Pre-alpha. APIs will change.

## Modules
- `:formflow-core` — pure Kotlin form engine (no Android/Compose)
- `:formflow-compose` — thin Jetpack Compose adapters
